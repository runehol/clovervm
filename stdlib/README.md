# CloverVM Standard Library

This directory contains ordinary Python modules loaded by clovervm's import
system. Trusted VM bootstrap code, such as the builtins module initializer, does
not live here.
