display_name: clovervm Performance Investigation
short_description: Benchmark and profile clovervm changes
default_prompt: 'Use $clovervm-performance-investigation to benchmark this clovervm change, compare it to a baseline, check hot opcode handler frame constraints, and report the evidence conservatively.'
